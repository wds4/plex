export const cgExample_101 = `const options = {
    roles = ["active","neuroCore-engine"]
}

const aResult = await cg.conceptGraphs.ls(options)
console.log(JSON.stringify(aResult,null,4))
// [ abc123, qwe234 ]
`
